@model IEnumerable<a12206149_ENT.Entities.Venta>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombreEmpleado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombreEmpleado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ventaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ventaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ventaId })
        </td>
    </tr>
}

</table>
