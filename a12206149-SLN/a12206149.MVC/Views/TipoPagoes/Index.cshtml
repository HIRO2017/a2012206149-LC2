@model IEnumerable<a12206149_ENT.ENT.Entities.TipoPago>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreTipoPago)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreTipoPago)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tipoPagoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.tipoPagoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tipoPagoId })
        </td>
    </tr>
}

</table>
