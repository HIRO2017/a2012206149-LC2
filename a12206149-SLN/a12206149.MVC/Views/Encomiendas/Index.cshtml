@model IEnumerable<a12206149_ENT.ENT.Entities.Encomienda>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipoEncomienda)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.destinoEncomienda)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipoEncomienda)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.destinoEncomienda)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.encomiendaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.encomiendaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.encomiendaId })
        </td>
    </tr>
}

</table>
